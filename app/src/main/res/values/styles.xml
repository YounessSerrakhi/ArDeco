<resources xmlns:tools="http://schemas.android.com/tools">

  <!-- Base theme for both light and dark themes -->
  <style name="Base.Theme.Decor" parent="Theme.MaterialComponents.DayNight.DarkActionBar" tools:ignore="DuplicateDefinition">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/lavender</item>
    <item name="colorPrimaryVariant">@color/lavender</item>
    <item name="colorOnPrimary">@color/white</item>
    <!-- Secondary brand color. -->
    <item name="colorSecondary">@color/teal_200</item>
    <item name="colorSecondaryVariant">@color/teal_700</item>
    <item name="colorOnSecondary">@color/black</item>
    <!-- Status bar color. -->
    <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
  </style>

  <!-- Light theme -->
  <style name="Base.Theme.Decor.Light" parent="Theme.MaterialComponents.Light.DarkActionBar" tools:ignore="DuplicateDefinition">
    <!-- Customize your light theme here. -->
    <item name="colorPrimary">@android:color/black</item>
    <!-- You can add more customization for the light theme if needed. -->
  </style>

  <!-- Dark theme -->
  <style name="Base.Theme.Decor.Dark" parent="Base.Theme.Decor" />

  <!-- Application theme -->
  <style name="AppTheme" parent="Base.Theme.Decor">
    <!-- All customizations that are NOT specific to a particular API-level can go here. -->
  </style>
  <style name="Theme.Decor" parent="Base.Theme.Decor" />

</resources>
